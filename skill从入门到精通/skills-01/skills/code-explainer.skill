---
name: code-explainer
description: 解释代码的功能和实现逻辑。在用户询问代码含义、代码分析、代码理解时使用（触发）。
---

# 代码解释器 (Code Explainer)

## Goal
帮助用户理解代码的功能、实现逻辑和最佳实践，提供清晰、易懂的代码解释。

## Workflow
1. **识别代码语言**: 自动识别代码使用的编程语言
   - 支持 Python、JavaScript、Java、C++、Go 等主流语言
   - 如果无法识别，主动询问用户

2. **分析代码结构**: 理解代码的整体结构和关键组件
   - 识别函数、类、模块等核心组件
   - 分析代码的执行流程和数据流
   - 识别关键算法和设计模式

3. **逐层解释**: 按照从宏观到微观的顺序解释代码
   - **宏观层面**: 代码的整体功能和目的
   - **中观层面**: 各个函数/类的作用和关系
   - **微观层面**: 关键代码行的具体逻辑

4. **补充说明**: 提供额外的有价值信息
   - 指出代码中的最佳实践或潜在问题
   - 解释使用的技术栈或库的作用
   - 如果有优化空间，给出建议

## Constraints
- 解释必须清晰易懂，避免使用过于专业的术语（除非必要）
- 对于复杂代码，使用类比或示例帮助理解
- 如果代码有明显错误或安全问题，必须明确指出
- 保持客观中立，不对代码质量做主观评价

## Examples

**Example 1: 简单函数解释**
User: "这段代码是做什么的？
```python
def fibonacci(n):
    if n <= 1:
        return n
    return fibonacci(n-1) + fibonacci(n-2)
```"

Response: "这是一个计算斐波那契数列的递归函数。它的功能是：
- 如果 n ≤ 1，直接返回 n（基础情况）
- 否则，返回前两项的和（递归情况）

例如：fibonacci(5) = fibonacci(4) + fibonacci(3) = 5

⚠️ 注意：这个实现虽然简洁，但效率较低（时间复杂度 O(2^n)），对于大数值会很慢。建议使用动态规划或迭代方式优化。"

**Example 2: 复杂代码分析**
User: "帮我分析这段代码的逻辑"

Response: "这段代码实现了一个 LRU（最近最少使用）缓存：

**整体功能**: 维护一个固定大小的缓存，当缓存满时自动淘汰最久未使用的数据。

**核心组件**:
1. `OrderedDict`: 保持插入顺序的字典，用于追踪访问顺序
2. `get()`: 获取数据并更新访问时间
3. `put()`: 插入数据，超出容量时删除最旧的项

**关键逻辑**:
- 每次访问时，将该项移到末尾（标记为最近使用）
- 插入新项时，如果超出容量，删除第一项（最久未使用）

这是一个经典的缓存淘汰算法，时间复杂度 O(1)。"

## Implementation Notes
- 对于超长代码（>100行），建议先让用户指定需要重点解释的部分
- 如果代码涉及特定领域知识（如机器学习、区块链），需要补充相关背景知识
